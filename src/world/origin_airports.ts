import type { LlaDeg } from "./enu";

// Default active airport origin (JFK).
// Source: authoritative docs (SkyNeedle Phase 1 defaults to JFK).
export const JFK_ORIGIN_LLA: LlaDeg = {
  latDeg: 40.6413,
  lonDeg: -73.7781,
  altM: 4
};


